# MeanFlow 单步从零强化学习算法

本文说明如何把当前稳定的 “schedule anchor PPO + 周期性 MeanFlow 自模仿” 思路迁移到四足、双足等运动控制任务。与机械臂 chunk policy 不同，运动控制不需要输出动作块；策略在每个环境步只根据当前观测输出当前动作。这里的 `sampling_steps=2` 只表示 MeanFlow 生成当前动作时内部走两段流，不表示连续执行两个环境动作。

## 核心设定

从零训练时，MeanFlow policy 和 critic 都随机初始化。每个环境 step 的流程是：

```text
obs_t -> MeanFlow policy -> action_t -> env.step(action_t)
```

强化学习按普通 step-level PPO 组织：

```text
rollout buffer:
    obs_t
    action_t
    reward_t
    done_t
    value_t
    old_anchor_loss_t,k
    old_meanflow_loss_t,k
    meanflow_t_t,k
    meanflow_r_t,k
    eps_t,k
advantage: A_t
return: R_t
```

算法分为两个阶段：

1. **Anchor PPO 自举**：用 schedule anchor loss 构造类似 log-prob 的 ratio，执行 PPO 更新。
2. **Elite MeanFlow 自模仿**：周期性筛选高优势或高回报 transition，用 MeanFlow residual 训练 actor，让策略更稳定地生成当前已发现的好动作。

## 与机械臂 Chunk 版本的区别

| 项目 | 机械臂版本 | 四足/双足从零版本 |
| --- | --- | --- |
| 策略输出 | 一个动作 chunk，例如 16 步 | 当前观测下的单步动作 |
| RL 粒度 | chunk-level PPO | step-level PPO |
| reward 聚合 | chunk 内折扣和 | 每个环境 step 的 reward |
| valid mask | chunk 提前 done 后屏蔽剩余动作 | done step 后按普通 rollout reset |
| Anchor energy 聚合 | 对 chunk 内有效动作步求和 | 当前 step 单动作，无 chunk 求和 |
| Elite 数据 | 成功轨迹或高优势 chunk | 高优势 transition 或高回报 episode transition |

## 推荐初始参数

对标当前稳定版本，保留两步 MeanFlow 生成和两步 schedule anchor，但去掉 chunk 相关设定：

```text
rollout_granularity = step
n_action_steps = 1
sampling_steps = 2
meanflow_fpo_loss_source = anchor
meanflow_anchor_sampling_mode = schedule
meanflow_anchor_sampling_steps = 2
n_action_samples = 4-8        # 从零阶段先小一些，稳定后可增到 16
trust_region_mode = ppo
clip_coef = 0.05-0.2
clamp_logratio = 3-5
gae_lambda = 0.95-0.99
gamma = 0.99
update_epochs = 4-8
num_minibatches = 4-16
meanflow_bc_update_mode = staged
meanflow_stage_update_order = anchor_first
meanflow_stage_sampling_mode = separate
meanflow_bc_stage_interval = 10
meanflow_bc_stage_rollout_repeats = 2-8
meanflow_bc_stage_epochs = 2-5
meanflow_bc_stage_selection = success_or_top 或 top_advantage
```

四足/双足建议使用 GPU 并行环境：

```text
num_envs = 1024-8192
rollout_length_per_env = 24-64
batch_steps = num_envs * rollout_length_per_env
```

动作空间应归一化到 `[-1, 1]`，再映射为关节位置、关节速度或 torque target。奖励需要足够 dense：

```text
r = r_velocity_tracking
    + r_alive
    + r_posture
    - c_torque * ||tau||^2
    - c_action_rate * ||a_t - a_{t-1}||^2
    - c_joint_limit * penalty
    - c_fall * fall
```

## MeanFlow 单步动作生成

当前策略只生成一个动作向量 `a_t`。设动作维度为 `D`，从高斯噪声初始化：

```text
x_1 ~ N(0, I), shape = (num_envs, action_dim)
```

两步 MeanFlow 生成：

```text
schedule = [1.0, 0.5, 0.0]

u_1 = u_theta(x_1, t=1.0, r=0.5, obs_t)
x_0.5 = x_1 - 0.5 * u_1

u_0.5 = u_theta(x_0.5, t=0.5, r=0.0, obs_t)
x_0 = x_0.5 - 0.5 * u_0.5

action_t = scale_and_clip(x_0)
```

如果使用 1 步或 4 步生成，anchor 的 schedule 必须同步：

```text
1 step:  [(1.0, 0.0)]
2 steps: [(1.0, 0.5), (0.5, 0.0)]
4 steps: [(1.0, 0.75), (0.75, 0.5), (0.5, 0.25), (0.25, 0.0)]
```

不要把动作生成设为两步，却让 anchor 使用随机 `t/r`。稳定版本能工作的关键就是：决策路径和 anchor ratio 使用同一套离散时间区间。

## Schedule Anchor Loss

对 rollout 中采到的单步动作 `x_data = action_t`，为每个 transition 采 `K=n_action_samples` 组噪声：

```text
eps_k ~ N(0, I), k = 1..K
v_k = eps_k - x_data
```

对每个噪声样本，固定遍历动作生成 schedule：

```text
for k in 1..K:
    for (t, r) in [(1.0, 0.5), (0.5, 0.0)]:
        x_t = (1 - t) * x_data + t * eps_k
        u = u_theta(x_t, t, r, obs_t)
        e_{k,t,r} = ||u - stopgrad(v_k)||^2

    anchor_loss_k = mean(e_{k,t,r} over schedule intervals)
```

rollout 时保存：

```text
obs_t
action_t
reward_t
done_t
value_t
old_anchor_loss[t, k]
old_meanflow_loss[t, k]
meanflow_t[t, k]
meanflow_r[t, k]
eps[t, k]
```

更新时复用同一批 `eps`，用当前策略重算 `curr_anchor_loss[t, k]`。这保证 old/new energy 是同一动作、同一噪声、同一 schedule 上的比较。

注意这里有两个不同的时间来源：

```text
Anchor ratio: 固定 schedule t/r，例如 [(1.0, 0.5), (0.5, 0.0)]
MeanFlow residual: rollout 时随机采样并保存的 meanflow_t / meanflow_r
```

二者共享同一批 `eps`，但用途不同。第一条稳定方案的 PPO ratio 只使用 schedule anchor；周期性自模仿阶段才使用随机 `meanflow_t/r/eps` 计算 MeanFlow residual。

## Anchor PPO Ratio

单步版本没有 chunk 内聚合，因此每个 transition 的 energy 就是：

```text
old_E_k = old_anchor_loss[t, k]
new_E_k = curr_anchor_loss[t, k]
logratio_k = old_E_k - new_E_k
logratio_k = clamp(logratio_k, -clamp_logratio, clamp_logratio)
ratio_k = exp(logratio_k)
```

解释：

```text
A_t > 0: 希望 new_E_k < old_E_k，让好动作更容易生成
A_t < 0: 希望 new_E_k > old_E_k，让坏动作更难生成
```

PPO 损失：

```text
A_t = normalize(advantage_t)
L1 = -A_t * ratio_k
L2 = -A_t * clamp(ratio_k, 1 - clip_coef, 1 + clip_coef)
policy_loss = mean(max(L1, L2) over t,k)
```

critic 损失：

```text
value_loss = 0.5 * mean((V(obs_t) - return_t)^2)
```

总损失：

```text
loss = policy_loss + vf_coef * value_loss
```

健康信号：

```text
logratio_pos_adv_mean > 0
logratio_neg_adv_mean < 0
policy_loss 长期为负
clipfrac 不应长期接近 1
```

## Elite MeanFlow 自模仿

对齐第一条稳定方案时，自模仿阶段应采用 **anchor_first + separate rollout**：

1. 先用主 rollout 完成 Anchor PPO 更新。
2. 若达到 `bc_interval`，用更新后的策略额外采样 `bc_rollout_repeats` 批数据。
3. 从这些新数据中筛选 elite transitions。
4. 只更新 actor，不更新 critic。

从零训练早期通常没有 success，因此不要依赖成功轨迹。先使用高优势或高 episode return 的 transition：

```text
if success_signal 可用且 batch 中已有足够成功 transition:
    selected = success transitions
else:
    selected = top_fraction by advantage 或 episode_return
```

推荐起点：

```text
top_fraction = 0.1-0.3
adv_weight = binary 或 relu
bc_loss_coef = 0.05-0.2
bc_interval = 5-20
bc_epochs = 2-5
bc_rollout_repeats = 2-8
```

MeanFlow 自模仿损失：

```text
x_t = (1 - t) * x_data + t * eps
v = eps - x_data

u, du_dt = jvp(u_theta, x_t, t, r; tangent=(v, 1, 0))
target = v - (t - r) * du_dt
L_meanflow = ||u - stopgrad(target)||^2
L_elite = weighted_mean(L_meanflow over selected transitions)
```

这个阶段的目标不是替代 PPO，而是把 Anchor PPO 已经找到的高回报动作压入 MeanFlow 动力学，让后续随机采样更容易落在好动作附近。

## 完整伪代码

```python
policy = MeanFlowPolicy(action_horizon=1, random_init=True)
critic = ValueNetwork(random_init=True)

for iteration in range(num_iterations):
    # ---------- Stage 1: step rollout ----------
    main_batch = collect_step_rollout(policy, critic, env)
    returns, advantages = compute_gae(main_batch, gamma=0.99, lambda_=0.95)
    main_batch.returns = returns
    main_batch.advantages = advantages

    # ---------- Stage 2: Anchor PPO ----------
    for epoch in range(update_epochs):
        for mb in minibatches(main_batch):
            curr_anchor = recompute_schedule_anchor(
                policy,
                obs=mb.obs,
                action=mb.action,
                eps=mb.eps,
                schedule=[(1.0, 0.5), (0.5, 0.0)],
            )

            logratio = clamp(mb.old_anchor - curr_anchor, -logratio_clip, logratio_clip)
            ratio = exp(logratio)

            A = normalize(mb.advantages)
            
            v_loss = mse(critic(mb.obs), mb.returns)
            update(policy, critic, pg_loss + vf_coef * v_loss)

    # ---------- Stage 3: separate MeanFlow self-imitation rollout ----------
    if (iteration + 1) % bc_interval == 0:
        bc_cache = []
        for repeat in range(bc_rollout_repeats):
            bc_batch = collect_step_rollout(policy, critic, env)
            bc_returns, bc_advantages = compute_gae(bc_batch, gamma=0.99, lambda_=0.95)
            bc_batch.returns = bc_returns
            bc_batch.advantages = bc_advantages
            bc_cache.append(bc_batch)

        # success_or_top: 有成功 transition 就优先成功，否则退化到 top advantage。
        selected_cache = select_elite_transitions(
            bc_cache,
            mode="success_if_available_else_top_advantage",
            top_fraction=0.2,
        )

        # ---------- Stage 4: MeanFlow self-imitation ----------
        for bc_epoch in range(bc_epochs):
            for mb in minibatches(selected_cache):
                meanflow_loss = recompute_meanflow_residual(
                    policy,
                    obs=mb.obs,
                    action=mb.action,
                    eps=mb.eps,
                    t=mb.meanflow_t,
                    r=mb.meanflow_r,
                )
                bc_loss = bc_loss_coef * weighted_mean(meanflow_loss, mb.weights)
                update_actor_only(policy, bc_loss)
```

其中 `collect_step_rollout` 需要同时保存 anchor 和 MeanFlow residual 所需的数据：

```python
def collect_step_rollout(policy, critic, env):
    rollout = []
    obs = env.reset_if_needed()

    for step in range(rollout_length_per_env):
        value = critic(obs)
        action = meanflow_sample_single_action(
            policy,
            obs,
            schedule=[1.0, 0.5, 0.0],
            zero_sampling=False,
        )

        # 一次调用中采 K 组 eps。anchor 使用固定 schedule；
        # MeanFlow residual 使用随机 meanflow_t / meanflow_r。
        old_meanflow, old_anchor, meanflow_t, meanflow_r, eps = compute_meanflow_and_anchor(
            policy_old=policy,
            obs=obs,
            action=action,
            n_action_samples=K,
            anchor_schedule=[(1.0, 0.5), (0.5, 0.0)],
        )

        next_obs, reward, done, info = env.step(action)
        rollout.append(
            obs=obs,
            action=action,
            reward=reward,
            done=done,
            value=value,
            old_meanflow=old_meanflow,
            old_anchor=old_anchor,
            meanflow_t=meanflow_t,
            meanflow_r=meanflow_r,
            eps=eps,
            info=info,
        )
        obs = reset_done_envs(next_obs, done)

    return rollout
```

## 实现分支

### 方案 A：纯在线

只从当前 rollout 中选 top transitions 做 MeanFlow 自模仿。

```text
优点：实现简单，无 replay stale 问题
缺点：没有对齐第一条稳定方案的 separate rollout；elite 数据少，早期方差大
```

### 方案 B：短期 Elite Buffer

维护最近若干轮 top transitions：

```text
buffer_size = 100k-1M transitions
sample_bias = newer data + higher return
store = obs, action, eps, t/r, advantage, return, done/info
discard = 过旧或低 return transition
```

这是四足/双足从零训练更推荐的版本。

### 方案 B2：对齐第一条的 Separate BC Cache

不维护长期 replay，只在每次触发 BC 时额外采 `bc_rollout_repeats` 批数据，缓存到内存，完成 `bc_epochs` 轮训练后丢弃。这是最接近第一条稳定方案的实现。

```text
优点：数据来自 Anchor PPO 更新后的当前策略，stale 程度低
缺点：采样成本高，约放大为 1 + bc_rollout_repeats / bc_interval
```

### 方案 C：课程训练

先让策略学会稳定站立和低速跟踪，再提高命令速度、扰动和地形难度：

```text
phase 1: alive + posture + low velocity
phase 2: command velocity tracking
phase 3: disturbance / terrain / domain randomization
```

## 最小可行配置

```text
action_horizon = 1
rollout_granularity = step
sampling_steps = 2
anchor_sampling_steps = 2
n_action_samples = 4
update_epochs = 5
num_minibatches = 8
clip_coef = 0.1
clamp_logratio = 3-5
bc_interval = 10
bc_epochs = 2
bc_rollout_repeats = 2-8
bc_loss_coef = 0.05
elite_top_fraction = 0.2
selection = top_advantage
```

当 return 稳定上升后，再逐步增加：

```text
n_action_samples: 4 -> 8 或 16
bc_epochs: 2 -> 5
bc_loss_coef: 0.05 -> 0.1/0.2
```

## 调参监控指标

这个算法调参时，最重要的不是先看成功率，而是确认 anchor ratio 是否真的把高优势动作推近。建议按下面优先级监控。

### 1. Anchor 方向

最关键的两个指标：

```text
logratio_pos_adv_mean > 0
logratio_neg_adv_mean < 0
```

含义：

```text
A_t > 0 的动作: 期望 new_anchor_loss < old_anchor_loss，所以 old - new > 0
A_t < 0 的动作: 期望 new_anchor_loss > old_anchor_loss，所以 old - new < 0
```

如果 `logratio_pos_adv_mean < 0`，说明策略正在远离好动作，主更新方向有问题。优先检查：

```text
anchor schedule 是否与动作生成 schedule 一致
old/new 是否复用同一批 eps
advantage 是否正确归一化
learning_rate_actor 是否过大
clip_coef / clamp_logratio 是否过紧或过松
```

### 2. Policy Loss

理想情况：

```text
policy_loss 长期为负，并且不要快速塌到 0 附近
```

如果 `policy_loss` 长期为正，通常表示更新方向有问题。如果一开始为负但很快平台化，说明 PPO clip、ratio 分布或学习率限制了继续优化。

### 3. Ratio 与 Logratio 分布

建议监控：

```text
logratio_mean
logratio_std
ratio_mean
ratio_std
clipfrac
```

经验判断：

```text
ratio_mean 接近 1 是正常现象
logratio_std 太小: 策略几乎没动
logratio_std 太大: 更新过猛
clipfrac 0.1-0.5 较健康
clipfrac 长期 > 0.6: clip 太紧或学习率太大
clipfrac 长期 < 0.05: 更新太弱
```

### 4. 任务回报与运动分解指标

四足/双足不要只看总 return。至少拆开监控：

```text
episode_return
fall_rate
episode_length
velocity_tracking_reward 或 velocity_tracking_error
base_height
base_orientation_error
energy / torque penalty
action_rate penalty
feet_contact 或 gait symmetry
```

如果 return 上升但动作抖动、能耗爆炸或姿态不稳，说明策略可能在钻 reward，需要加强动作平滑、能耗、姿态或接触相关惩罚。

### 5. Critic 质量

建议监控：

```text
value_loss
explained_variance
returns_mean
values_mean
advantages_mean
advantages_std
```

如果 critic 不准，anchor PPO 的 advantage 就是噪声。危险信号包括：

```text
explained_variance 长期接近 0 或为负
advantages_std 极大
value_loss 爆炸
```

这时应降低 actor 学习率、增加 critic update，或在前几轮只训练 critic / 弱化 actor 更新。

### 6. Elite / Self-Imitation 阶段质量

如果开启 MeanFlow 自模仿，重点监控：

```text
elite_selected_fraction
elite_mean_return
elite_mean_advantage
bc_loss
bc_loss_coef
bc_updates
```

早期 self-imitation 不宜过强。危险信号：

```text
elite_return 没上升，但 bc_loss 很强
BC 后 return 明显下降
action 分布快速变窄，探索消失
```

对应处理：

```text
降低 bc_loss_coef
减少 bc_epochs
减少 bc_rollout_repeats
延后 bc_start_iteration 或增大 bc_interval
```

### 7. 动作分布与探索

必须监控：

```text
action_mean
action_std
action_abs_mean
action_clip_fraction
init_noise_scale
sample diversity
```

如果 `action_std` 很快塌缩，从零训练会失去探索。如果 `action_clip_fraction` 很高，说明动作尺度、actor_scale 或动作映射范围不合适。

### 8. 最小判断顺序

每次调参先按这个顺序判断：

```text
1. logratio_pos_adv_mean 是否 > 0，logratio_neg_adv_mean 是否 < 0
2. policy_loss 是否长期为负
3. clipfrac / logratio_std 是否处在健康范围
4. critic explained_variance 是否上升
5. episode_return 和运动分解指标是否同步改善
6. elite_mean_return 是否上升，BC 是否破坏探索
```

如果 `policy_loss` 为负但 return 不升，通常是 reward、动作尺度或 exploration scale 不合适。如果 `logratio_pos_adv_mean < 0`，优先检查 anchor schedule 是否与动作生成 schedule 对齐，以及 old/new 是否复用同一批 `eps`。



