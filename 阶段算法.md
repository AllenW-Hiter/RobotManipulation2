# MeanFlow 分阶段在线强化微调算法

本文记录第一条长跑指令对应的实际算法，便于在其他任务上复刻。该方法不是完整 optimizer resume，而是从上一次训练得到的 MeanFlow policy checkpoint 加载权重，重新开始在线 RL 训练。

## 核心思想

策略仍然以 chunk 为动作单位。每个 chunk 包含 `n_action_steps=16` 个环境单步动作，强化学习的 advantage、return 和 PPO 更新都按 chunk 组织。策略生成动作时使用 MeanFlow 两步采样：从高斯噪声出发，沿 `[1.0, 0.5, 0.0]` 两个区间生成动作块。

训练分为两个阶段：

1. **Anchor PPO 阶段**：每轮采集一批 chunk rollout，用 schedule anchor loss 构造类 log-prob ratio，执行 PPO 更新。
2. **MeanFlow BC 阶段**：每隔若干轮，在更新后的策略上额外采样多批 rollout，筛选成功轨迹或高优势 chunk，用 MeanFlow residual 做模仿学习。

## 关键参数

第一条指令的主要设置如下：

```text
num_envs = 16
data_collection_steps = 3008
n_action_steps = 16
sampling_steps = 2
n_action_samples = 16
update_epochs = 10
num_minibatches = 8
clip_coef = 0.1
clamp_logratio = 5
clamp_old_anchor_loss = 4
meanflow_fpo_loss_source = anchor
meanflow_anchor_sampling_mode = schedule
meanflow_anchor_sampling_steps = 2
meanflow_bc_update_mode = staged
meanflow_stage_update_order = anchor_first
meanflow_stage_sampling_mode = separate
meanflow_bc_stage_interval = 10
meanflow_bc_stage_rollout_repeats = 8
meanflow_bc_stage_epochs = 5
meanflow_bc_stage_selection = success_or_top
meanflow_bc_stage_top_fraction = 0.2
meanflow_bc_stage_adv_weight = binary
meanflow_bc_stage_loss_coef = 0.5
```

当前代码中，`--policy meanflow` 默认启用 `meanflow_use_stable_defaults=True`。如果这些参数仍保持通用默认值，程序会自动切换到上面这套稳定配置；若命令行显式传入某个参数，则以命令行为准。需要完全关闭该 preset 时，传入：

```text
--meanflow-use-stable-defaults False
```

每轮主 rollout 的 chunk 数为：

```text
chunks_per_env = data_collection_steps / n_action_steps = 188
batch_chunks = chunks_per_env * num_envs = 3008
```

## MeanFlow 动作生成

给定观测 `obs`，策略先编码观测，再从高斯噪声初始化 `x_1`：

```text
x_1 ~ N(0, I)
for (t, r) in [(1.0, 0.5), (0.5, 0.0)]:
    u = u_theta(x_t, t, r, obs)
    x_r = x_t - (t - r) * u
action_chunk = unnormalize(actor_scale * x_0)
```

这里 `action_chunk` 的形状为 `(num_envs, 16, action_dim)`。环境仍然单步执行动作，但训练时把这 16 步视为一个伪动作。

## Rollout 数据组织

每个 chunk 起点保存一次观测和 value：

```text
value_start = V(encode(obs_start))
action_chunk = policy(obs_start)
old_anchor_loss = AnchorLoss(policy_old, obs_start, action_chunk)
old_meanflow_loss, t, r, eps = MeanFlowLoss(policy_old, obs_start, action_chunk)
```

随后依次执行 chunk 内动作。如果中途 episode 结束，则剩余动作位置标记为 invalid，不参与 loss 聚合。

chunk 回报使用折扣和：

```text
R_chunk = sum_{k=0}^{L-1} gamma^k * reward_k
discount_chunk = gamma^L
```

其中 `L` 是该 chunk 的有效执行长度。GAE 和 return 都按 chunk 级 value 序列计算。

## Schedule Anchor Loss

Anchor loss 用于把 MeanFlow 的平均速度约束到当前采样动作对应的真实方向。第一条稳定配置的关键点是：**动作生成使用两步 MeanFlow，anchor 也使用同一组两步离散 schedule**。也就是说，anchor 的 `(t, r)` 不是随机采样，而是固定为：

```text
[(1.0, 0.5), (0.5, 0.0)]
```

这由以下参数共同决定：

```text
sampling_steps = 2
meanflow_anchor_sampling_mode = schedule
meanflow_anchor_sampling_steps = 2
```

对 replay action `x_data` 和一组噪声 `eps`：

```text
v = eps - x_data
for (t, r) in [(1.0, 0.5), (0.5, 0.0)]:
    x_t = (1 - t) * x_data + t * eps
    u = u_theta(x_t, t, r, obs)
    loss_anchor_step = ||u - stopgrad(v)||^2
loss_anchor = mean(loss_anchor_step over schedule steps)
```

这里的 `n_action_samples=16` 表示每个 chunk 会采 16 组 `eps`。每组 `eps` 都会在两个 schedule 区间上计算 anchor loss，然后对两个区间求均值。因此 old anchor loss 的有效结构可以理解为：

```text
old_anchor_loss[action_step, env, noise_sample]
```

其中每个元素已经包含两段 schedule 的平均值。rollout 时会保存 old anchor loss 和 `eps`；更新时复用同一批 `eps`，但重新用当前策略计算 deterministic schedule anchor loss，从而保证 old/new 是同一噪声样本上的对比。

注意：代码中同时保存的 `cfm_loss_t/r` 主要服务于 MeanFlow residual/BC 阶段。schedule anchor 自身的 `(t, r)` 来自 `meanflow_anchor_sampling_steps` 构造的离散网格，而不是 replay 的随机 `cfm_loss_t/r`。

## Anchor PPO 更新

对 minibatch 中每个 chunk，先聚合 old/current anchor loss。当前配置 `do_average_cfm_loss_in_chunk=False`，因此先对有效动作步求和，再保留 16 个 sample 的估计：

```text
old_E = AggregateValidSteps(old_anchor_loss)
new_E = AggregateValidSteps(curr_anchor_loss)
logratio = old_E - new_E
logratio = clamp_ste(logratio, -5, 5)
ratio = exp(logratio)
```

若 chunk 内提前 done，done 之后的动作位置会被 `valid_mask=0` 屏蔽。当前稳定配置不对 chunk 内动作步求平均，而是求和：

```text
E = sum_{valid action step} mean_over_anchor_schedule(||u_theta - v||^2)
```

这样 16 步 chunk 会被当成一个宏动作，PPO ratio 对整个动作块负责。

直觉上，若某个 chunk advantage 为正，PPO 会推动 `new_E < old_E`，使策略更容易生成该 chunk；若 advantage 为负，则倾向于降低该 chunk 的生成倾向。

策略损失采用标准 PPO clipped objective：

```text
A = normalize(chunk_advantage)
L1 = -A * ratio
L2 = -A * clamp(ratio, 1 - clip_coef, 1 + clip_coef)
policy_loss = mean(max(L1, L2))
```

critic 同一阶段更新：

```text
value_loss = 0.5 * mean((V(obs_start) - return_chunk)^2)
total_loss = policy_loss + value_loss
```

每轮执行 `update_epochs=10`，每轮切成 `num_minibatches=8` 个 minibatch。

## MeanFlow BC 阶段

每隔 `meanflow_bc_stage_interval=10` 轮触发一次。因为 `stage_update_order=anchor_first` 且 `stage_sampling_mode=separate`，流程是：

1. 先用主 rollout 数据完成 Anchor PPO 更新。
2. 用更新后的策略重新采样 `meanflow_bc_stage_rollout_repeats=8` 批 rollout。
3. 把这些 rollout 缓存在内存中。
4. 只对 actor 执行 `meanflow_bc_stage_epochs=5` 轮 MeanFlow BC。

BC 样本选择规则为 `success_or_top`：

```text
if cache 中存在成功 episode:
    selected = 属于成功 episode 的 chunk
else:
    selected = advantage 排名前 20% 的 chunk
weights = 1 if selected else 0
```

BC 损失使用 MeanFlow residual，而不是 anchor loss：

```text
u, du_dt = jvp(u_theta, x_t, t, r; tangent=(v, 1, 0))
target = v - (t - r) * du_dt
L_meanflow = ||u - stopgrad(target)||^2
L_bc = 0.5 * weighted_mean(L_meanflow over selected chunks)
```

这里复用 rollout 时存储的 `t/r/eps`，使 BC 阶段学习的是被选中 chunk 上的 MeanFlow 一致性，而不是重新随机采样目标。

## 完整伪代码

```python
policy = load_meanflow_checkpoint(checkpoint="oqajwx9d:latest", load_ema=True)
critic = init_critic()

for iteration in range(num_iterations):
    # ---------- Stage 1: 主 rollout ----------
    batch = []
    for chunk_idx in range(data_collection_steps // n_action_steps):
        obs0 = env.current_obs()
        value0 = critic(encode(obs0))

        action_chunk = meanflow_sample(
            policy,
            obs0,
            schedule=[1.0, 0.5, 0.0],
            init="gaussian",
        )

        old_anchor, old_meanflow, t, r, eps = compute_losses(
            policy,
            obs0,
            action_chunk,
            n_action_samples=16,
            anchor_mode="schedule",
            anchor_steps=2,
        )

        rewards, dones, valid_mask, success_episode_ids = env.step_chunk(action_chunk)
        batch.append(obs0, action_chunk, rewards, dones, valid_mask,
                     value0, old_anchor, old_meanflow, t, r, eps,
                     success_episode_ids)

    returns, advantages = compute_chunk_gae(batch, gamma=0.99, lambda_=0.99)

    # ---------- Stage 2: Anchor PPO ----------
    for epoch in range(10):
        for minibatch in split(batch, num_minibatches=8):
            curr_anchor = recompute_anchor_loss(
                policy,
                minibatch.obs,
                minibatch.actions,
                t=minibatch.t,
                r=minibatch.r,
                eps=minibatch.eps,
            )

            old_E = aggregate_valid(minibatch.old_anchor, minibatch.valid_mask)
            new_E = aggregate_valid(curr_anchor, minibatch.valid_mask)
            logratio = clamp(old_E - new_E, -5, 5)
            ratio = exp(logratio)

            A = normalize(minibatch.chunk_advantage)
            pg_loss = ppo_clipped_loss(ratio, A, clip=0.1)
            v_loss = mse(critic(encode(minibatch.obs)), minibatch.returns)

            update(policy, critic, pg_loss + v_loss)

    # ---------- Stage 3: 周期性 MeanFlow BC ----------
    if iteration % 10 == 0:
        bc_cache = []
        for repeat in range(8):
            bc_batch = collect_rollout_with_current_policy(policy, critic)
            bc_cache.append(bc_batch)

        for epoch in range(5):
            for minibatch in iterate_minibatches(bc_cache):
                selected = select_success_or_top_advantage(
                    minibatch,
                    top_fraction=0.2,
                )
                if selected.sum() == 0:
                    continue

                meanflow_loss = recompute_meanflow_residual(
                    policy,
                    minibatch.obs,
                    minibatch.actions,
                    t=minibatch.t,
                    r=minibatch.r,
                    eps=minibatch.eps,
                )
                bc_loss = 0.5 * weighted_mean(meanflow_loss, selected)
                update_actor_only(policy, bc_loss)
```

## 复刻到其他任务时的注意事项

1. `n_action_steps` 最好与 BC policy 的 horizon 对齐；本实验为 16。
2. `sampling_steps` 应与 BC 基线一致；这里使用 S2 MeanFlow，所以动作生成和 schedule anchor 都是 2 步。
3. 如果任务奖励稀疏且成功信号可靠，优先使用 `success_or_top`；没有成功轨迹时才退化到 top advantage。
4. `policy_loss` 应长期保持负值，且 `meanflow_anchor/logratio_pos_adv_mean > 0`、`meanflow_anchor/logratio_neg_adv_mean < 0` 是 anchor 工作的关键信号。
5. BC 阶段会显著增加采样成本；本设置每 10 轮额外采 8 批 rollout，实际环境步数大约放大为 `1 + 8/10 = 1.8` 倍。
